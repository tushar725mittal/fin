import 'package:flutter/material.dart';

class Therapist extends StatelessWidget {
  const Therapist({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("This is therapist pg"),),
    );
  }
}